import styled from 'styled-components';
import theme from '../../../../config/utils/theme';

const LogoTitleStyle = styled.h1`
	font-family: Lato;
	font-size: 40px;
	font-weight: bold;
	font-stretch: normal;
	font-style: normal;
	line-height: normal;
	letter-spacing: normal;
	text-align: left;
	color: ${theme.color.primary};
	margin: 0;
`;

export default LogoTitleStyle;
